import { defineConfig } from 'astro/config';

// If you deploy to a custom domain (e.g. andriimaksymenko.dev), set:
// site: 'https://andriimaksymenko.dev'
export default defineConfig({
  site: 'https://andriimaksymenko.dev',
});
